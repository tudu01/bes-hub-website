// styles/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#388e3c', // Match Tailwind primary color
    },
    secondary: {
      main: '#f57c00', // Match Tailwind secondary color
    },
  },
});

export default theme;
