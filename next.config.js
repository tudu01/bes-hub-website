/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    typescript: {
      // Note: During development, you might want to ignore TypeScript errors
      ignoreBuildErrors: true,
    },
  }
  
  module.exports = nextConfig